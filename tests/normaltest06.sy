// Error type 6

int array[10];

void func1() {}
void func2() {}

int main(){
    int apple = 1;
    int i = 1;
    int f = 3;
    i + array; // int + arr
    f - i;
    array[0] = i + 100;
    if(i > 100){
        i = i + array[0];
    }
    else if (0 || array){ // int || arr
        array/i; // arr / int
    }
    else if (f != array) { // int != arr
        apple*f;
    }
    else if (array <= i){ // arr <= int
        apple+apple;
        array-array; // arr - arr
        apple * i;
    }

    if (f && f) {
        while(!array) { // !arr
            i - f;
        }
    }

    func1 + func2; // 
    func1 + array; // 
    func1 - 0; //
}

